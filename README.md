# Experimental stuff for Mind Technology Lab

Quick proof-of-concept stuff for the lab.

Requires Python 3.10+ (tooling targets 3.12). Install with [uv](https://docs.astral.sh/uv/):

```bash
uv sync --extra test
uv run pre-commit install
uv run pytest
```
