# User role vector dashboard

Next.js UI for the mini-replication of arXiv:2609.00608.

The charts mix published tables with a CPU planted-direction run. The steering lab swaps prewritten messages; it does not load Qwen.

```bash
npm install
npm run dev
```

Vercel should treat this folder as the project root (`framework: nextjs` in `vercel.json`).
