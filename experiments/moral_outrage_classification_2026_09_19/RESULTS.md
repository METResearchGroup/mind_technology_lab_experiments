# Results

Smoke did not finish. Perspective AnalyzeComment returned HTTP 400 `Unknown requested attribute: MORAL_OUTRAGE`. The smoke process exited non-zero and did not retry with `TOXICITY`. The 1,000-row jobs have not started.

Sample comparison tables are empty until a successful smoke table is approved and Step 7 runs.
